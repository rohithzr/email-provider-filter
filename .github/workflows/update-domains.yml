name: Update Email Domain Lists

on:
  schedule:
    - cron: '0 6 * * *'  # Run daily at 6 AM UTC
  push:
    branches: [ main ]
  workflow_dispatch:  # Allow manual trigger

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        persist-credentials: true
        
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests
        
    - name: Run aggregation script
      run: python scripts/aggregate.py
      
    - name: Check for changes
      id: changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add output/
        if git diff --staged --quiet; then
          echo "changed=false" >> $GITHUB_OUTPUT
          echo "No changes detected in output files"
        else
          echo "changed=true" >> $GITHUB_OUTPUT
          echo "Changes detected in output files"
          # Set tag name for release
          echo "tag_name=v$(date +'%Y%m%d-%H%M')" >> $GITHUB_OUTPUT
          echo "release_name=Email Domains $(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT
          # Read delta stats
          if [ -f output/delta.json ]; then
            echo "total_added=$(jq -r '.total_added' output/delta.json)" >> $GITHUB_OUTPUT
            echo "total_removed=$(jq -r '.total_removed' output/delta.json)" >> $GITHUB_OUTPUT
            echo "total_domains=$(jq -r '.total_domains' output/delta.json)" >> $GITHUB_OUTPUT
            echo "disposable_added=$(jq -r '.categories.disposable.added' output/delta.json)" >> $GITHUB_OUTPUT
            echo "disposable_removed=$(jq -r '.categories.disposable.removed' output/delta.json)" >> $GITHUB_OUTPUT
            echo "disposable_total=$(jq -r '.categories.disposable.total' output/delta.json)" >> $GITHUB_OUTPUT
            echo "free_added=$(jq -r '.categories.free.added' output/delta.json)" >> $GITHUB_OUTPUT
            echo "free_removed=$(jq -r '.categories.free.removed' output/delta.json)" >> $GITHUB_OUTPUT
            echo "free_total=$(jq -r '.categories.free.total' output/delta.json)" >> $GITHUB_OUTPUT
            echo "paid_personal_added=$(jq -r '.categories.paid_personal.added' output/delta.json)" >> $GITHUB_OUTPUT
            echo "paid_personal_removed=$(jq -r '.categories.paid_personal.removed' output/delta.json)" >> $GITHUB_OUTPUT
            echo "paid_personal_total=$(jq -r '.categories.paid_personal.total' output/delta.json)" >> $GITHUB_OUTPUT

            # Build sample lists for release notes (up to 20 each)
            ADDED_LIST=$(jq -r '.added_samples[]' output/delta.json | sed 's/^/- `/' | sed 's/$/`/' | head -20)
            REMOVED_LIST=$(jq -r '.removed_samples[]' output/delta.json | sed 's/^/- `/' | sed 's/$/`/' | head -20)

            # Use multiline outputs via delimiter
            echo "added_list<<ADDEDEOF" >> $GITHUB_OUTPUT
            echo "$ADDED_LIST" >> $GITHUB_OUTPUT
            echo "ADDEDEOF" >> $GITHUB_OUTPUT

            echo "removed_list<<REMOVEDEOF" >> $GITHUB_OUTPUT
            echo "$REMOVED_LIST" >> $GITHUB_OUTPUT
            echo "REMOVEDEOF" >> $GITHUB_OUTPUT
          fi
        fi
        
    - name: Commit and push changes
      if: steps.changes.outputs.changed == 'true'
      run: |
        git commit -m "Update email domain lists - $(date '+%Y-%m-%d')"
        git push
        
    - name: Create release
      if: steps.changes.outputs.changed == 'true'
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.changes.outputs.tag_name }}
        release_name: ${{ steps.changes.outputs.release_name }}
        body: |
          Daily update of email domain classifications

          ## Changes
          | Category | Added | Removed | Total |
          |----------|-------|---------|-------|
          | Disposable | +${{ steps.changes.outputs.disposable_added }} | -${{ steps.changes.outputs.disposable_removed }} | ${{ steps.changes.outputs.disposable_total }} |
          | Free | +${{ steps.changes.outputs.free_added }} | -${{ steps.changes.outputs.free_removed }} | ${{ steps.changes.outputs.free_total }} |
          | Paid Personal | +${{ steps.changes.outputs.paid_personal_added }} | -${{ steps.changes.outputs.paid_personal_removed }} | ${{ steps.changes.outputs.paid_personal_total }} |
          | **Total** | **+${{ steps.changes.outputs.total_added }}** | **-${{ steps.changes.outputs.total_removed }}** | **${{ steps.changes.outputs.total_domains }}** |

          <details><summary>Domains Added (up to 20)</summary>

          ${{ steps.changes.outputs.added_list }}

          </details>

          <details><summary>Domains Removed (up to 20)</summary>

          ${{ steps.changes.outputs.removed_list }}

          </details>

          ## Generated Files
          - `email_domains.json` - Complete dataset in JSON format
          - `email_domains.csv` - Complete dataset in CSV format
          - `disposable.txt` - Disposable email domains only
          - `free.txt` - Free email provider domains only
          - `paid_personal.txt` - Paid personal email provider domains only

          ---
          Sources: [disposable-email-domains](https://github.com/disposable-email-domains/disposable-email-domains), [mailchecker](https://github.com/FGRibreau/mailchecker), [burner-email-providers](https://github.com/wesbos/burner-email-providers), and curated additions.
        draft: false
        prerelease: false
        
    - name: Job Summary
      if: steps.changes.outputs.changed == 'true'
      run: |
        echo "### Email Domain Lists Updated! 🎉" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Dataset Statistics:**" >> $GITHUB_STEP_SUMMARY
        echo "- **Total Domains:** $(cat output/email_domains.json | jq -r '.metadata.total_domains') domains" >> $GITHUB_STEP_SUMMARY
        echo "- **Disposable:** $(cat output/email_domains.json | jq -r '.metadata.categories.disposable') domains" >> $GITHUB_STEP_SUMMARY  
        echo "- **Free:** $(cat output/email_domains.json | jq -r '.metadata.categories.free') domains" >> $GITHUB_STEP_SUMMARY
        echo "- **Paid Personal:** $(cat output/email_domains.json | jq -r '.metadata.categories.paid_personal') domains" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Generated Files:**" >> $GITHUB_STEP_SUMMARY
        echo "- [\`email_domains.json\`](https://github.com/Rohithzr/email-provider-filter/blob/main/output/email_domains.json) - Complete dataset in JSON format" >> $GITHUB_STEP_SUMMARY
        echo "- [\`email_domains.csv\`](https://github.com/Rohithzr/email-provider-filter/blob/main/output/email_domains.csv) - Complete dataset in CSV format" >> $GITHUB_STEP_SUMMARY
        echo "- [\`disposable.txt\`](https://github.com/Rohithzr/email-provider-filter/blob/main/output/disposable.txt) - Disposable email domains only" >> $GITHUB_STEP_SUMMARY
        echo "- [\`free.txt\`](https://github.com/Rohithzr/email-provider-filter/blob/main/output/free.txt) - Free email provider domains only" >> $GITHUB_STEP_SUMMARY
        echo "- [\`paid_personal.txt\`](https://github.com/Rohithzr/email-provider-filter/blob/main/output/paid_personal.txt) - Paid personal email provider domains only" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Release:** [\`${{ steps.changes.outputs.tag_name }}\`](https://github.com/Rohithzr/email-provider-filter/releases/tag/${{ steps.changes.outputs.tag_name }})" >> $GITHUB_STEP_SUMMARY
        
    - name: No Changes Summary
      if: steps.changes.outputs.changed == 'false'
      run: |
        echo "### No Changes Detected ✨" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "All domain sources were checked but no updates were found." >> $GITHUB_STEP_SUMMARY
        echo "The existing dataset remains current." >> $GITHUB_STEP_SUMMARY